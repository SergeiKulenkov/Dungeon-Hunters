using UnityEngine;

[CreateAssetMenu(fileName = Definitions.RANGER_CONFIG,
menuName = Definitions.SCRIPTABLE_OBJECTS_PATH + Definitions.PLAYER + "/" + Definitions.RANGER_CONFIG)]
public class RangerSO : PlayerSO
{
    
}
